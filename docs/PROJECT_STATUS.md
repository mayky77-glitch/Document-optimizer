# Статус проекта

## Блок 1

- **Номер:** 1
- **Название:** Каркас проекта и инвентаризация источников
- **Статус:** реализован, локальные тесты пройдены; GitHub CI проверяется в PR
- **Вход:** путь к каталогу, обычному файлу или ZIP-архиву
- **Выход:** `FileManifest` и атомарный UTF-8 JSON
- **Версия схемы:** `1.0`

## Публичный API

- `classify_file_by_name(filename)`
- `scan_directory(directory_path, recursive=True)`
- `scan_zip_archive(archive_path, ...)`
- `build_file_manifest(source_path, recursive=True)`
- `save_manifest_json(manifest, output_path)`
- `load_manifest_json(input_path)`
- `FileClassification`
- `FileManifestEntry`
- `ManifestSummary`
- `FileManifest`
- `StatusCode`

## Тесты

Покрыты:

- классификация и комбинированные маркеры;
- временные файлы, копии и устаревшие версии;
- рекурсивный и нерекурсивный обход;
- символические ссылки и детерминированный порядок;
- ZIP Slip, повреждённый ZIP, размер и коэффициент сжатия;
- отсутствие распаковки;
- восстановление UTF-8 имён ZIP без UTF-8-флага;
- дубли;
- атомарная запись;
- сериализация и десериализация;
- публичный контракт источника с `FileManifest`;
- CLI-сценарий ZIP → манифест.

## Результат на реальных данных

Проверен пользовательский ZIP размером 238 633 974 байта без распаковки:

- 177 файловых записей;
- 46 Excel/ODS-файлов (`.xlsx` и `.xlsb`);
- 124 PDF;
- 24 временные или macOS-служебные записи;
- 3 вероятные копии;
- 0 опасных путей;
- 0 нечитаемых записей;
- SHA-256 до и после обработки совпал.

Реальные данные были предоставлены отдельно от пустого центрального
репозитория и не добавлены в Git. Детальная статистика фиксируется в описании
Pull Request.

## Ограничения

- содержимое Excel и PDF не читается;
- листы и строки не обрабатываются;
- индексы документов ещё не нормализуются;
- исходные файлы не изменяются;
- ZIP не распаковывается;
- вычисления и сопоставление работ отсутствуют.

## Зависимость следующего блока

Следующий блок должен принимать готовый `FileManifest`, сохранять `file_id` и
provenance и не выполнять повторную инвентаризацию.

## Блок 2

- **Номер:** 2
- **Название:** Извлечение и нормализация индексов документов
- **Статус:** интегрирован локально; ожидает финального review и публикации
- **Вход:** готовый `FileManifest` или его JSON-представление
- **Выход:** `FileManifest` схемы `2.0` с индексными полями и сводкой

Блок распознаёт строгие индексы `1006 (682)`, сохраняет ведущие нули, отмечает
неоднозначность и низкую уверенность, не читает Excel и не меняет источник.
Команды: `inventory --extract-indexes` и `extract-indexes`.

### Подтверждение на approved corpus

30 июля 2026 года выполнены обе команды на каталоге
`/Users/x/Documents/Сооотношение документов`: 6 180 файлов, включая 382 Excel,
60 подтверждённых индексов, 0 неоднозначных и 1 низкоуверенный. SHA-256 всех
382 Excel-файлов до и после совпали; JSON-манифесты записывались только в `/tmp`.

## Commit

SHA реализации указывается в Pull Request после публикации ветки.

## Блок 3

- **Номер:** 3
- **Название:** Детерминированный выбор файла-источника
- **Статус:** интегрирован локально; ожидает финального review и публикации
- **Вход:** `FileManifest` и `SourceSelectionRequest`
- **Выход:** `SourceSelectionResult` с выбранным кандидатом, рейтингом,
  отклонениями и объяснением

Блок не открывает Excel и не изменяет исходные документы. Он обогащает копию
манифеста периодом, редакцией и статусами имени, затем применяет фильтры и
детерминированное ранжирование. CLI: `enrich-metadata` и `select-source`.

## Блок 4

- **Номер:** 4
- **Название:** Безопасная материализация и чтение Excel-книги
- **Статус:** интегрирован локально; ожидает финального review и публикации
- **Вход:** выбранный `SourceCandidate` из блока 3
- **Выход:** `MaterializedSource`, `DualWorkbookSession`, метаданные книги и листов

`inspect-workbook` безопасно открывает `.xlsx`/`.xlsm` в двух read-only
представлениях: формулы и сохранённые значения. Для ZIP извлекается только
выбранная запись в изолированную временную область с проверкой пути, размера и
CRC; после закрытия сессии область очищается. Неподдерживаемые `.xls`, `.xlsb`
и `.ods` отклоняются до открытия. JSON выбора теперь включает полную
JSON-совместимую запись источника, поэтому его можно передать в
`inspect-workbook --selection` без повторного сканирования.

### Подтверждение на новых реальных данных

30 июля 2026 года API `prepared_workbook_session` материализовал и открыл пять
разных документов: КС-2, КС-6а, СВВР, реестр и сводный отчёт. Все пять были
открыты как Office Open XML; SHA-256 каждого исходного файла до и после совпал.
Подробный воспроизводимый журнал находится в карточке интеграции блока 4.

## Блок 5

- **Номер:** 5
- **Название:** Распознавание структуры Excel-книги
- **Статус:** интегрирован локально; ожидает финального review и публикации
- **Вход:** `DualWorkbookSession` блока 4
- **Выход:** `WorkbookSchema`, `WorksheetSchema`, `ColumnResolution`

`detect-schema` ограниченно читает только диагностическое окно листа,
классифицирует типы КС-2/КС-3/КС-6а/СВВР и связанных листов, находит заголовки
и сопоставляет логические столбцы. Исходная книга не изменяется.

## Блок 6

- **Номер:** 6
- **Название:** Извлечение канонических строк и публичные адаптеры
- **Статус:** реализован локально; P6-ремедиации staged, ожидают focused review; ещё не закоммичен и не опубликован
- **Версия пакета:** `0.6.0`
- **Вход:** манифест или selection, `WorkbookSchema` блока 5 и read-only сессия блока 4
- **Выход:** канонические строки, JSONL/JSON и `*.meta.json` с атомарной записью

Публичные адаптеры и extraction поддерживают КС-2, КС-6а и СВВР. Каждая строка
сохраняет raw/cached/formula значения и provenance источника и ячейки. Схема
блока 5 управляет извлечением блока 6; неразрешённые и не-OK столбцы
пропускаются, malformed schema обрабатывается контролируемо. Формулы не
вычисляются, текст `ArrayFormula` нормализуется.

`extract-rows` принимает manifest или selection вместе со schema/output и
поддерживает фильтры sheet/type, limits и форматы вывода. Потоковые JSONL и
meta, а также JSON, записываются атомарно с возможностью восстановления.

При неопределённой схеме или отсутствии поддерживаемого листа выполняется
контролируемый нулевой результат либо отказ для ручной проверки; данные не
угадываются. Жизненный цикл источника остаётся read-only для `.xlsx`/`.xlsm`
и безопасно выбранной записи ZIP.

### Проверки блока 6

- точный gate 1006: путь на 12 787 строк;
- положительные KS-2, KS-6a и combined-сценарии с неизменным SHA источника;
- полный набор: **336 passed, 1 skipped**;
- slow 50k gate пройден после P6-ремедиации.
